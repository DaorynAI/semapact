apiVersion: v3.1.0
kind: DataContract
id: orders-contract
name: orders-contract
version: 1.0.0
status: active
domain: commerce
description:
  purpose: Updated descriptive documentation for orders contract
schema:
  - id: orders
    name: orders
    physicalName: tbl_orders
    description: Orders table description update
    properties:
      - id: id
        name: id
        physicalName: col_id
        logicalType: string
        physicalType: varchar(64)
        required: true
        description: Primary identifier updated notes
      - id: amount
        name: amount
        physicalName: col_amount
        logicalType: number
        physicalType: decimal(10,2)
        required: false
        description: Order total amount in USD
